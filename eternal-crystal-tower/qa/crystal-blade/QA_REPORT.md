# 晶刃、模块攻击特效与三种小怪验证

日期：2026-09-12。环境：Windows、Node.js 24.10.0、Playwright Chromium、本地服务 `http://127.0.0.1:4173`。

本次模型与特效改动的检查通过；未对整款游戏重新作完整通关验收。

## 实现范围

- 晶刃：六翼有厚度的晶体转子，接入环绕、弹射、返程和回收，塔上发生器共用造型。
- 新增小怪：墨影猎犬、轨道微星、锈蚀甲虫。代码中另有瓷甲守卫未建模；按本次三种的工作范围保留原表现。
- 八种模块：攻击或防御事件驱动的短促爆发，独立武器瞄准，拆卸后停止绘制。
- 导出：独立晶刃及三种小怪 GLB，更新模块晶刃 GLB。

## 自动检查

- `npm test`：243 / 243 通过，包含固定种子复现、完整构筑模拟、15 分钟压力模拟及现有模块、渲染、第二章回归。
- 最后调整火焰朝向与护盾事件后，`node --test tests/combat-models.test.js tests/module-effects.test.js`：6 / 6 通过。
- 新模型具有有限顶点、单位法线、非退化三角面和三个方向的体积；GLB 头、文件长度、顶点数和二进制顶点与运行时一致。
- 无 WebGL 的软件绘制通过；晶刃旋转与返程复用已缓存网格。
- 攻击事件仅激活对应模块，护盾使用 `sectorBlock`；特效数量和路径长度有界、衰减后回到待机、不改变输入事件或模块数据。
- `git diff --check` 无空白错误。

## 浏览器证据

1. `/combat-model-preview.html`：四种新模型均显示，真实点击暂停旋转有效。放大预览采用更高离屏分辨率。1440 宽与 1024×720 下无横向溢出。
2. `/module-model-preview.html`：点击“演示模块攻击”并切换配置，确认炮口喷焰、晶刃弧、出舱光、霜晶、炉焰与雷弧。截图在短促爆发阶段冻结浏览器动画以便观察；战场仍使用真实事件。
3. `/?preview=astral-enemies&seed=42`：用现有调试状态和安装函数装上合法的轻炮、晶刃、霜、火、雷六格配置；调整敌人位置和生命作可重复观测，使用真实“继续／暂停战斗”按钮推进。时间由 270 到 274.17 秒，目标生命由 100000 降至 99469.55；记录到开火、晶刃命中、元素命中和反应。三个新类型均调用 `EnemyModelRenderer`；晶刃绘制随运行持续发生。
4. 使用现有专精函数开启回旋飞刃，再以按钮推进：累计观察到 3 次发射、6 次弹射、3 次返程、1 次回归爆发和回收事件，确认同一个新网格进入完整飞行流程。
5. 使用现有 `forceGameOver` 验证失败界面，然后真实点击“再燃晶塔”。剧情自动结束后进入新局：生命 600、只有初始轻炮、晶刃升级为 0、飞刃列表为空；新局推进到 61.67 秒仍在运行。1024×720 下页面宽度为 1024，无横向溢出。
6. 战场页面无控制台错误。首次模型预览有原有 `favicon.ico` 404，未影响模型或关键资源加载。

截图均位于 `qa/screenshots/`：

- `crystal-blade-new-enemies.png`：模型对照。
- `module-attacks-hive.png`：模块攻击爆发。
- `combat-models-before.png`、`combat-models-after.png`：战斗推进前后。
- `combat-models-1024.png`、`combat-restart-1024.png`：较小视口和重开。

## 验证边界

本次未改变伤害、碰撞、刷怪或存档规则。现有密集怪潮的图集回退保留，第二章舰种继续使用原有表现。未做移动端 GPU 压力实测、全流程人工通关或新的平衡性评价。
